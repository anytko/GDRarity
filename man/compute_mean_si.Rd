% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_mean_si.r
\name{compute_mean_si}
\alias{compute_mean_si}
\title{Compute Mean Scarcity Index (Si) for Each Species}
\usage{
compute_mean_si(
  data,
  species_col = "species",
  site_col = "site",
  abundance_col = "abundance"
)
}
\arguments{
\item{data}{A data frame containing species, site, and abundance information.}

\item{species_col}{Name of the column in \code{data} indicating species identity. Defaults to \code{"species"}.}

\item{site_col}{Name of the column in \code{data} indicating site identity. Defaults to \code{"site"}.}

\item{abundance_col}{Name of the column in \code{data} containing species abundance per site. Defaults to \code{"abundance"}.}
}
\value{
A data frame with species and their corresponding mean Si values.
}
\description{
This function calculates the average scarcity index (Si) for each species across sites using relative abundance data and the \pkg{funrar} package's \code{scarcity_stack()} function.
}
\details{
It first computes relative abundances within each site, applies \code{scarcity_stack()} from the \pkg{funrar} package, and then averages the Si values for each species.

This function uses \code{funrar::scarcity_stack()} to compute the scarcity index (Si) per species per site based on relative abundance, and then returns the average Si per species.
}
\note{
This function requires the \pkg{funrar} package. If you use this function in published work, please cite the \pkg{funrar} package using: \code{citation("funrar")}.
}
\examples{

df <- data.frame(
  species = c("sp1", "sp2", "sp1", "sp3", "sp2", "sp3"),
  site = c("A", "A", "B", "B", "C", "C"),
  abundance = c(10, 5, 3, 8, 2, 7)
)
compute_mean_si(df)

}
