% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/restriction_performance.r
\name{run_all_gdrare_combos}
\alias{run_all_gdrare_combos}
\title{Run All Restrictions of the Global Model of Discretized Rarity}
\usage{
run_all_gdrare_combos(
  species_df,
  abundance_df = NULL,
  trait_cols = NULL,
  geo_methods = c("taxonomic", "range"),
  fun_methods = c("min_distance", "mean_distance"),
  abundances = c(TRUE, FALSE),
  threshold_sets = list(list(GR = 0.15, GL = 0.15, FR = 0.9, FL = 0.9, PR = 0.75, PL =
    0.75), list(GR = 0.15, GL = 0.15, FR = 0.75, FL = 0.75, PR = 0.75, PL = 0.75),
    list(GR = 0.15, GL = 0.15, FR = 0.9, FL = 0.9, PR = 0.9, PL = 0.9), list(GR = 0.15,
    GL = 0.15, FR = 0.9, FL = 0.9, PR = 0.75, PL = 0.75)),
  direction_sets = list(list(GR = "low", GL = "low", FR = "high", FL = "high", PR =
    "high", PL = "high"), list(GR = "low", GL = "low", FR = "high", FL = "high", PR =
    "high", PL = "high"), list(GR = "low", GL = "low", FR = "high", FL = "high", PR =
    "high", PL = "high"), list(GR = "low", GL = "low", FR = "high", FL = "high", PR =
    "high", PL = "high")),
  species_col = "species",
  use_internal_phylo = TRUE,
  internal_phylo_name = "ALLMB",
  k_means = FALSE,
  additional_dimensions = NULL,
  model = NULL,
  phylo = NULL,
  verbose = TRUE
)
}
\arguments{
\item{species_df}{Data frame of species trait and metadata.}

\item{abundance_df}{Data frame of site-by-species abundance data.}

\item{trait_cols}{Character vector of trait column names to use.}

\item{geo_methods}{Character vector of methods to use for regional geographic rarity. Default: c("taxonomic", "range").}

\item{fun_methods}{Character vector of methods to use for regional functional rarity. Default: c("min_distance", "mean_distance").}

\item{abundances}{Logical vector indicating whether to include abundance in local phylogenetic rarity calculations. Default: c(TRUE, FALSE).}

\item{threshold_sets}{List of named numeric vectors defining rarity thresholds for each combination. Must be the same length as \code{direction_sets}.}

\item{direction_sets}{List of named character vectors ("high"/"low") defining threshold directions for each combination. Must be the same length as \code{threshold_sets}.}

\item{species_col}{Name of the species identifier column in data frames. Default: "species".}

\item{use_internal_phylo}{Logical, whether to use an internal seed plant phylogeny. Default: TRUE.}

\item{internal_phylo_name}{Name of the internal seed plant phylogeny to use if applicable. Default: "ALLMB". Options: "ALLMB", "ALLOTB", "GBMB", "GBOTB".}

\item{k_means}{Logical, whether to apply k-means clustering. Default: FALSE.}

\item{additional_dimensions}{Optional list of additional custom rarity axes to include in the analysis.}

\item{model}{Optional specific restriction to pass through the pipeline. Default: NULL.}

\item{phylo}{An optional phylogenetic tree of class \code{phylo}. If provided and \code{use_internal_phylo} is \code{FALSE}, this tree will be used for phylogenetic rarity calculations.}

\item{verbose}{Logical, whether to print progress messages. Default: TRUE.}
}
\value{
A list with elements:
\itemize{
\item results: List of data frames with rarity categorizations for restrictions per method combo.
\item threshold_sets: Data frame summarizing threshold sets used.
\item direction_sets: Data frame summarizing direction sets used.
\item param_grid: Data frame with the full parameter grid combinations.
}
}
\description{
Runs the gdrare pipeline for all restrictions. Returns the list of dataframes with joined original species data, summaries of thresholds, directions, and the parameter grid.
}
